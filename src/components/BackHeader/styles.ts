import { StyleSheet } from 'react-native'

// styles
import { spaces } from '../../theme'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  label: {
    marginLeft: spaces.baseSpace * 3,
  },
})

export default styles
